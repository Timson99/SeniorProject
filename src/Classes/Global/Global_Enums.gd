extends Object

class_name Enums

enum Dir {Up, Down, Left, Right}
enum Move {Attack, Skills, Items, Defend, Run}
enum Elementals { Ice, Fire, Electric }
enum SkillPower { Weak, Medium, Strong }
enum SkillType { Spell, Force, Choice, Text }
